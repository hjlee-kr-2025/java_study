Execute클래스를 만든이유?
1. 개발자가 디버깅하기 쉽게
2. 실행되는 서비스를 확인하기 위해
3. 서비시 실행의 정보확인(데이터)
4. 서비스 실행시간 측정

Execute의 리턴Type
- Object
==> 모든자료형에 대응하기 위해
- 자녀클래스는 부모클래스로
변수선언을 할 수 있습니다(클래스다형성)
- Object는 모든클래스의
부모클래스 입니다.

Execute의 매개변수
(Service service, Object obj)
- 프로그램구성시 모든 종류의 서비스는
Service 인터페이스를 상속받아서
구현합니다.
- Object도 모든자료형을 전달변수로
사용가능하도록 하기 위해 선언


